{
  "name": "extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension for Chrome Bridge MCP",
  "scripts": {
    "build": "tsc -p tsconfig.json && rm -rf dist/icons && cp -R icons dist/icons && cp manifest.json popup.html dist/"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.13"
  }
}
